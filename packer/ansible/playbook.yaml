---
# WordPress ECS — Ansible Playbook
# Runs inside the Docker container provisioned by Packer.

- name: Provision WordPress container image
  hosts: all
  become: true
  gather_facts: false

  # Role variables are defined in roles/wordpress/vars/main.yml
  roles:
    - role: wordpress